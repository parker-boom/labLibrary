# Lab Library Polish Fix List - 2026-05-24

Scope: keep the approved design, content structure, routes, ordering, and component hierarchy intact. Make only the fixes below.

## 1. Home Page
- Lower the `Lab Archive` title plus the three selection cards slightly so the group feels more centered in the viewport.
- Make the portal and camera icon halos match the corrected joystick halo, with complete circular shapes and no clipped white edges.
- Rename the home title from `Lab Archive` to `Lab Arcade`.
- Change the second card subtext from `LEVEL UP WITH STUDENT EXAMPLES.` to `UPGRADE WITH STUDENT EXAMPLES`.
- Remove the periods from the three home card subtitles.
- Add one subtle idle animation in the empty space near the `Lab Arcade` heading, with the motion traveling purposefully from the title edge toward the right side of the archive-card dead space.
- Add a restrained route transition when opening the three subpages from the home page, only if it stays quick and native to the current arcade style.

## 2. Missions Page
- Make the page title slightly smaller.
- Make the page subtitle slightly larger.
- Reduce the vertical gap between the header area and the mission options.

## 3. Workflows Page
- Apply the same page-title, subtitle, and below-header gap adjustments as the Missions page.

## 4. Reports Page
- Apply the same page-title, subtitle, and below-header gap adjustments as the Missions page.

## 5. Missions Popup
- Restore a visible opening animation so the popup does not snap open.
- Tighten the spacing in the `Best for` card.
- Make the related event image a little taller.
- Shorten the related event school/college label so it does not feel overly long.
- Remove the text that says `3 examples to plug into the room`.

## 6. Workflows Popup
- Restore a visible opening animation so the popup does not snap open.

## 7. Reports Popup
- Restore a visible opening animation so the popup does not snap open.
